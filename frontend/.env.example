# Backend API URL (for production deployment)
# In development, the app uses Vite proxy to localhost:8000
# In production (Render), set this to your backend URL
VITE_API_URL=https://taskflow-backend-0dax.onrender.com
